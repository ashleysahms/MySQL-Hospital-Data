CREATE TABLE patient_contact_log
(
patient_id varchar(10),
patient_phone_number varchar(10)
);

CREATE TABLE check_in_update
(
patient_id varchar(10),
provider_id varchar(10),
room_number varchar(10)
);
